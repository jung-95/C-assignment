#include <stdio.h>
int main(void)
{
	int i, n;
	printf("n = ");
	scanf_s("%d", &n); 

	for (i = 0; i < n; i++)
	{
		if (i % 2 == 0)
		{
			printf("0");
		}

		else
		{
			printf("X");
		}
	}
	printf("\n");

	return 0;
}