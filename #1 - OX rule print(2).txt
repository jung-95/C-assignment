#include <stdio.h>
int main(void)
{
	int i, n;
	printf("n = ");
	scanf_s("%d", &n); 

	for (i = 0; i < n * n; i++)
	{
		if ((i / n) % 2 == 0)
		{
			printf("O");
		}

		else
		{
			printf("X");
		}
	}

	printf("\n");
	return 0;
}